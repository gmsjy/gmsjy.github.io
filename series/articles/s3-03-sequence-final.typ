// s3-03-sequence-final.typ — 第三季 03：数列与不等式压轴：放缩、归纳、构造
#import "../template.typ": *
#import "../figures/s3-fig-03-sequence-final.typ": fig as fig-cobweb
#import "../figures/s3-fig-03b-bound.typ": fig as fig-bound
#import "../figures/s3-fig-03c-structure.typ": fig as fig-structure

#show: article.with(
  [数列与不等式压轴：放缩、归纳与构造],
  season: "第三季 · 压轴与先修",
  module: "高考压轴 · 03 / 18",
  minutes: 15,
  words: "约 2800 字",
)

#section("痛点引子", "「每一项都比前一项小，所以和小于……小于多少？」")

数列压轴的标志题型：证明 $1 + 1/(2^2) + 1/(3^2) + … + 1/n^2 < 2$。直觉上「每项都小、和当然有限」，可直觉不能当证明。真正的证明要靠一个精巧的动作——*放缩*：把每项换成稍大但可求和的项 $1/(n(n-1))$，望远镜相消后剩下常数。

放缩、归纳、构造，是数列压轴的三件兵器。它们与导数压轴（第三季第 1 篇）并列高考数学最难两顶王冠。这一篇给三件兵器各配一副使用说明书。

#section("知识地图", "从「求和」到「估和」")

高二已学：等差等比、递推构造、数学归纳法（第二季第 3 篇）。压轴新增：*放缩法*（把难求和的项替换成可求和的项）、*夹逼与单调性*（用不动点思想定位数列行为）、*数列不等式*（先猜后证）。断层在哪？常规求和求「精确值」，压轴求和只求「卡在某个界内」——*从精确到估计*的思维转变，是数列压轴的门槛。

#figure-block("3-1", [递推蛛网：不动点是放缩的「靶子」], fig-cobweb)

递推数列 $a_(n+1) = f(a_n)$ 的行为可以画出来：从 $a_1$ 出发，在曲线与对角线之间画「梯子」，数列的走势一目了然。图中 $f(x) = 0.25x + 2$ 的不动点 $x = 8/3$ 像一块磁铁——数列单调逼近它。*不动点给出「界」在哪里，放缩给出「界」为什么成立*，两者配合就是数列不等式的完整答案。

#section("核心讲解", "两个核心：放缩的拼图术与构造的定位术")

#strong[核心概念一：放缩 = 把「不可求和」换成「可求和」。]

#figure-block("3-2", [放缩与夹逼：矩形柱卡在两条曲线之间], fig-bound)

放缩的素材库有三格。*裂项格*：$1/(n(n+1)) = 1/n - 1/(n+1)$，望远镜相消——把 $1/n^2$ 放缩为 $1/(n(n-1))$（$n >= 2$）正是借这一格求和。*等比格*：$1/n^2 <= 1/2^(n-1)$（$n >= 4$ 附近起效），把余项换成等比和——处理「只要证明小于某常数」的题格外顺手。*均值格*：$a + b >= 2 sqrt(a b)$，在数列中制造可求和的下界。三格共用的心法：*方向别放反*（证上界要放大、证下界要缩小），*放缩要留有余地*（别一步放到临界值上，后面的项会溢出）。

#strong[核心概念二：构造 = 先认结构再动手。]

#figure-block("3-3", [数列压轴三件套：结构决定解法], fig-structure)

递推压轴的第一问几乎总是「构造新数列」：对比标准等比型，用待定系数补一个常数。构造完成后，通项立现，第二问的不等式才有落脚点。若递推是 $f(a_n)$ 型（如 $a_(n+1) = sqrt(a_n + 2)$），则走不动点路线：解 $f(x) = x$ 找不动点，证明数列单调有界——「单调 + 有界 → 收敛」是压轴论述的标准句式。无论哪条路线，收尾都交给*数学归纳法*：构造出结论与证明结论成立是两问，阅卷分开给分。

#section("例题拆解", "一道完整压轴的三问走线")

#problem-box([例题], [数列 $a_n$ 满足 $a_1 = 1$，$a_(n+1) = (2 a_n) / (a_n + 2)$。(1) 求 $b_n = 1 / a_n$ 的通项；(2) 证明 $a_n < 2 / n$。])

*第一问（构造）*：取倒数——$1 / a_(n+1) = (a_n + 2) / (2 a_n) = 1/2 + 1 / a_n$。于是 $b_(n+1) = b_n + 1/2$，$b_1 = 1$，等差！$b_n = 1 + (n - 1) / 2 = (n + 1) / 2$，故 $a_n = 2 / (n + 1)$。

*第二问（比较）*：$a_n = 2 / (n + 1) < 2 / n$，因为 $n + 1 > n$。一句放缩收工——但注意，原题若改为证明 $a_1 dot a_2 … dot a_n > (2 / (n+1))^n$ 之类，就需要归纳法配合。

*复盘*：整道题的流程是「认结构（倒数是等差）→ 构造 → 得通项 → 一句放缩」。压轴题的可怕在于第一眼，可怕之处恰恰催生解法：*结构认得越快，构造越早发生*。而「认结构」的能力，来自第二季第 3 篇的四类标准型训练——压轴是常规方法的极限测试，不是新知识考试。

数列不等式还有一条「先紧后松」的操作细则值得演练：放缩的方向要与求和的「望远镜」配合。证明 $sum 1/n^2 < 2$ 的经典走线是：$1/n^2 < 1/(n(n-1)) = 1/(n-1) - 1/n$（$n >= 2$），相消后剩 $1 + 1 = 2$。注意首项 1 要单独保留——它放缩不动，硬放进裂项会出负数。由此提炼操作细则：*首项单列、从第二项放缩、放缩后必须恰好可裂项或可等比*。三步的先后次序错一步，整道题重写。这类「精细度」正是数列压轴区分度所在。

#section("思维方法", "估计思维：从精确到界的跨越")

#method-box([「不必算准，只须卡住」], [
微积分诞生前，数学家处理无穷和的方式正是放缩与夹逼——祖冲之算圆周率、阿基米德算抛物线面积，都是这套思想的祖先。

数列压轴训练的核心能力是：*在没有精确解的世界里，给出可靠的界*。这也是数值分析、算法复杂度理论的日常。当你能坦然写下「$S_n < 2$，证毕」，你已经跨进了高等数学的门槛。
])

#section("行动清单", "今天就能做")

不动点思想还有一层「与函数压轴的联动」值得点破：数列递推 $a_(n+1) = f(a_n)$ 的不动点，就是方程 $f(x) = x$ 的根——于是数列问题与导数零点问题共享同一副骨架（第三季第 1 篇的零点讨论在这里二次上岗）。强基笔试更喜欢把两者焊在一起：先证不动点存在（零点定理）、再证数列收敛于不动点（单调有界）、最后估计收敛速度（与不动点的距离每轮折半）。这条「递推 → 不动点 → 收敛」的三级链，是连接高中数列与大学迭代理论的主干道。

#strong[快问快答：三个高频疑问]
- *放缩放过头能补救吗？* 答：能。换一个「更紧」的放缩（如从 $1/n^2 <= 1/n$ 换成 $1/(n(n-1))$），或保留首项单独处理。
- *归纳法的传递步证不出来怎么办？* 答：通常说明构造的通项错了——回头验证 $n = 2, 3$ 是否满足；递推题先构造后归纳正是为此。
- *不动点方程没有解怎么办？* 答：数列无水平渐近——改考察周期性或单调发散，压轴问法会相应变为「证明发散」或「求界」。

#action-box((
  [① 把放缩素材库三格（裂项、等比、均值）各配一个使用例句，做成一张「弹药卡」。],
  [② 完整重做上面的例题，并用数学归纳法独立证明 $a_n = 2 / (n + 1)$（构造与证明分开写）。],
  [③ 给 $a_(n+1) = sqrt(2 + a_n)$、$a_1 = sqrt(2)$ 画递推蛛网图，猜出不动点并用归纳法证明 $a_n < 2$。],
))
