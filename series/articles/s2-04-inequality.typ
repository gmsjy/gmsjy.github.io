// s2-04-inequality.typ — 第二季 04：不等式与最值：均值、柯西与线性规划
#import "../template.typ": *
#import "../figures/s2-fig-04-inequality.typ": fig as fig-lp
#import "../figures/s2-fig-04b-mean.typ": fig as fig-mean
#import "../figures/s2-fig-04c-compare.typ": fig as fig-compare

#show: article.with(
  [不等式与最值：均值、柯西与线性规划],
  season: "第二季 · 高中深化",
  module: "数学深化 · 04 / 18",
  minutes: 13,
  words: "约 2600 字",
)

#section("痛点引子", "「用了一次基本不等式，答案却是错的」")

求 $f(x) = x + 4/x$（$x > 0$）的最小值，同学小林写：$x + 4/x >= 2 sqrt(4) = 4$，所以最小值是 4。判卷给了零分——因为取等条件 $x = 2$ 时 $f(2) = 4$，明明没错呀？再看下一题：求 $x + 4/x$（$x >= 5$）的最小值，他还写 4，这次真错了：$x$ 取不到 2，取等不成立。

基本不等式不难，难在*取等条件*。这一篇把不等式三大利器讲清楚，重点是每件利器的「使用说明书」。

#section("知识地图", "从解不等式到造最值")

初中已学：一元一次不等式（组）的解法、数轴表示。高中要求：基本不等式及其变形、柯西不等式的思想、线性规划、以及「已知不等式求最值」的完整范式。断层在哪？初中不等式是「解出范围」，高中不等式是「证明与优化」——工具从「移项合并」升级为「结构观察 + 定理套用」。

#figure-block("4-1", [线性规划：可行域与目标函数等值线], fig-lp)

线性规划是不等式的应用化身：几条约束围出可行域（多边形），目标函数 $z = x + 2y$ 的等值线是一族平行线，*沿着法线方向推到最远*，最优解出现在顶点。把约束、目标一分开，这类题立刻失去神秘感——高中试卷的送分题之一。

#section("核心讲解", "两个核心：均值不等式的几何本质与使用纪律")

#strong[核心概念一：$(a + b) / 2 >= sqrt(a b)$ 是一个几何事实。]

#figure-block("4-2", [均值不等式的几何证明：半径 ≥ 半弦], fig-mean)

半圆直径 $"AB" = a + b$，分点 $C$ 把直径分成 $a$ 与 $b$ 两段。过 $C$ 立一根垂直半弦 $"CD" = sqrt(a b)$（相交弦定理），而圆心到圆周的最大距离是半径 $(a + b)/2$。*半径永远不小于半弦*，这便是最重要不等式的全部证明。图里还藏着取等条件：当且仅当 $C$ 与圆心重合，即 $a = b$ 时，半弦升到半径——「正用取最值、逆用验存在、取等定结果」三句话的前两句都写在图上。

#figure-block("4-3", [三组数的算术平均与几何平均对比], fig-compare)

柱状图看得更直观：$a, b$ 越悬殊，两个平均的差距越大；两者相等时柱子等高。这个「差距」在应用题里对应着*资源的浪费程度*——当面积一定时，正方形周长最短，本质也是这一条。

#strong[核心概念二：使用基本不等式的三条纪律。]

其一，*正*：各项必须为正，负数直接套用必错。其二，*定*：积或和必须是定值——求 $x + 4/x$ 最小值时积 $x dot 4/x = 4$ 是定值，这正是能用它的前提；若和与积都不定，先凑系数（配凑法）。其三，*等*：取等条件必须在你要求的范围内取得到。开头小林的第二问正是栽在第三条上：$x >= 5$ 时取不到 $x = 2$，此时该用单调性而不是均值不等式。三字诀「正定等」，缺一不可。

至于柯西不等式 $ (a_1 b_1 + a_2 b_2)^2 <= (a_1^2 + a_2^2)(b_1^2 + b_2^2) $，高中只要求思想与简单应用：它的几何身世是「夹角余弦绝对值不超过 1」，是数量积性质的直接推论——记住这一句，形式再复杂的柯西也不过是一条向量不等式的换装。

#section("例题拆解", "一道最值题的三种武器")

#problem-box([例题], [已知 $x > 0$，$y > 0$，且 $2x + y = 4$，求 $1/x + 2/y$ 的最小值。])

*武器一：常数代换（1 的妙用）*。把 1 写成 $(2x + y)/4$：

$ 1/x + 2/y = ((2x + y) / 4)(1/x + 2/y) = 1/4 (4 + (2y)/x + (2x)/y + 4) >= 1/4 (8 + 2 sqrt(4)) = 3 $

取等条件 $x = y = 4/3$，在约束内成立 ✓。

*武器二：消元 + 对勾函数*。由 $y = 4 - 2x$ 代入，$f(x) = 1/x + 2/(4 - 2x)$，用对勾函数的图像与单调区间求最小值。

*武器三：线性规划思想（参数化）*。把 $2x + y = 4$ 看成直线上的动点，目标函数视为关于点的函数，用图像定位最优。

三种武器殊途同归，最小值都是 3。考试时首选武器一（最快），武器二是万能保底——*构造不出来时，老老实实消元永远不算错*。

基本不等式还有一个「姐妹不等式」值得同框记忆：$a^2 + b^2 >= 2 a b$（平方版本），它与均值版本 $a + b >= 2 sqrt(a b)$（正数版本）互相推导、各有战场。平方版不需要正数条件、适用面更广，证明垂直与极值时常更顺手；均值版直接对接「和定积最大、积定和最小」两大应用范式。另外，「和定积最大」的推广形态（三个正数和一定时积最大，均取等）是多项式最值与高阶放缩的常用武器，强基笔试里的常客。

#section("思维方法", "优化思维：约束下的极致")

#method-box([最值问题的统一视角], [
所有最值问题都是同一个故事：在约束围出的范围里找目标函数的极值点。不等式武器是「一步到位的 shortcut」，图像与消元是「永远可行的 long way」。

先用 shortcut，验证三字诀；shortcut 失效立刻切 long way。能把「快方法」与「稳方法」分层调用的学生，才叫真正掌握了不等式。
])

#section("行动清单", "今天就能做")

线性规划还有一个容易被忽略的「整数解」变体：可行域顶点算出的最优解若是小数，而实际问题要求整数（人数、件数），就必须在可行域内*寻找最近的整点*——沿目标函数等值线往回收，第一个碰到的整点才是答案。这是新高考应用题的新宠，也是「数学模型回到现实」的绝佳注脚：纸面上的最优解，要经过现实约束的最后一道打磨。

#strong[快问快答：三个高频疑问]
- *均值不等式能用在不等号一边吗？* 答：可以，但取等条件必须落在题目要求的范围内，否则结论作废。
- *柯西不等式高考能直接用吗？* 答：新高考教材已含（三维形式），使用时标注即可；老大纲地区建议先证后用。
- *可行域的顶点一定是整数吗？* 答：不一定。整数规划要在顶点附近「找整点」，这正是它的额外难度所在。

#action-box((
  [① 用「一正二定三相等」检查你最近做过的三道基本不等式题，标出取等条件并验证其在范围内。],
  [② 用「1 的代换」重解上面例题，完整写出取等条件求解 $x, y$ 的过程。],
  [③ 画出一道双约束线性规划的可行域（自拟约束），用顶点法求目标函数最大最小值各一次。],
))
