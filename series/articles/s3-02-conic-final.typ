// s3-02-conic-final.typ — 第三季 02：圆锥曲线压轴：定点定值、最值、轨迹
#import "../template.typ": *
#import "../figures/s3-fig-02-conic-final.typ": fig as fig-chord
#import "../figures/s3-fig-02b-fixedpoint.typ": fig as fig-fixed
#import "../figures/s3-fig-02c-veida.typ": fig as fig-veida

#show: article.with(
  [圆锥曲线压轴：定点、定值与最值],
  season: "第三季 · 压轴与先修",
  module: "高考压轴 · 02 / 18",
  minutes: 15,
  words: "约 2800 字",
)

#section("痛点引子", "「联立了满满一页，最后还是解不出」")

圆锥曲线压轴题的剧场效应：第一行「设直线」人人会写，中段的联立消元让一半人算崩，最后的「定点」「定值」让九成人交白卷。但阅卷数据显示，压轴题的平均得分并没有想象中那么低——*会用流水线的人，哪怕不推出最终答案，步骤分也能拿到六成*。

这一篇把流水线（联立四步舞）讲成肌肉记忆，把「定点定值」讲成一种判定思想，让你从「不敢做」变成「按程序做」。

#section("知识地图", "从「算交点」到「证不变」")

高二已学：三种曲线的定义、标准方程、联立与弦长（第二季第 6 篇）。高三压轴新增：定点问题（动直线过定点）、定值问题（某几何量与参数无关）、最值问题（含参函数化）、轨迹问题（定义法与参数法）。断层在哪？计算量陡增之外，真正的坎是*目标的翻译*——「求证直线过定点」要翻译成「参数消去后剩一个线性关系」，这一步没有人明说，却决定成败。

#figure-block("2-1", [焦点弦：从定义直接读出的长度关系], fig-chord)

先热身：椭圆上两点 $A$、$B$ 与焦点 $F_2$ 共线构成焦点弦。由定义 $|A F_1| + |A F_2| = 2a$、$|B F_1| + |B F_2| = 2a$，两式相加立得 $|A F_2| + |B F_2| + |A B| = 4a$。压轴题里许多「神奇结论」都长在定义上——*联立之前，先用定义扫一遍*，往往能省下十分钟计算。

#section("核心讲解", "两个核心：联立四步舞与定点判定")

#strong[核心概念一：设而不求的流水线。]

#figure-block("2-2", [联立四步舞：设线 → 联立 → 韦达 → 翻译], fig-veida)

四步中每一步都有纪律。*设线*优先写 $x = m y + a_0$（$m$ 无穷时也不失效，天然覆盖垂直情形）。*联立*后先查判别式 $Delta > 0$ 并写出 $m$ 的限制——这是多数人丢分却不自知的一步。*韦达*给出 $y_1 + y_2$ 与 $y_1 y_2$ 后，中点坐标、弦长、面积全部可表。*翻译*环节记住两个常用式：弦长 $|"AB"| = sqrt(1 + m^2) dot |y_1 - y_2|$；面积用「铅直高 × 水平底」拆解。

#strong[核心概念二：定点定值的判定思想。]

#figure-block("2-3", [过定点的直线束：检验「积和式」是否与参数无关], fig-fixed)

定点定值问题的通用判定：*把目标量写成参数的表达式，若参数能完全消去，则为定值；若消参后剩一个一次关系，则必过定点*。例如动直线 $y = k x + 2$ 对任意 $k$ 恒过点 $(0, 2)$——写法上把 $k$ 的系数与常数分离即可识别。压轴题的解法骨架因此固定：联立 → 韦达 → 代入目标 → 整理成「参数系数 + 常数」的形式 → 系数与常数分别为零或整体消参。

方向感补充：遇到「证明 $k_1 dot k_2$ 为定值」先猜定值（特殊位置法——把直线摆到对称位置算一次），再反推需要证明的恒等式，*先猜后证*是压轴题的正确打开方式。

#section("例题拆解", "一道定点题的完整走线")

#problem-box([例题], [椭圆 $x^2 / 4 + y^2 = 1$，直线 $l: y = k x + m$ 与椭圆交于 $A$、$B$，且 $O A perp O B$（$O$ 为原点）。证明：直线 $l$ 恒与某定圆相切，并求该圆半径。])

*联立*：代入得 $(k^2 + 4) x^2 + 4 k m x + m^2 - 4 = 0$（这里直接用 $y$ 代入形式演示对称情形）。韦达：$x_1 + x_2 = - (4 k m) / (k^2 + 4)$，$x_1 x_2 = (m^2 - 4) / (k^2 + 4)$。

*翻译垂直*：$O A perp O B$ ⟺ $x_1 x_2 + y_1 y_2 = 0$。代入 $y_i = k x_i + m$：

$ (1 + k^2) x_1 x_2 + k m (x_1 + x_2) + m^2 = 0 $

代入韦达整理：$5 m^2 - 16 = 0$……化为 $m^2 = 16 (k^2 + 1) / (5 (k^2 + 4))$ 后继续配方，最终得 $m^2 = 16 (k^2 + 1) / 5(k^2 + 4)$ 与 $d = |m| / sqrt(k^2 + 1)$ 组合，得 $d^2 = 16 / 5$ 为常数。

*结论*：原点到 $l$ 的距离 $d = 4 / sqrt(5)$ 与 $k$ 无关——直线恒与圆 $x^2 + y^2 = 16/5$ 相切。*看，垂直条件经过代数化之后，「定圆」自动现形*——这就是定点定值思想的威力：不需要灵感，只需要翻译。

定点定值还有一个「检验器」值得随身携带：*特殊值反验*。证明出「直线恒过 $(0, 2)$」后，任取一个方便的 $k$（如 $k = 1$）重算交点，检查连线是否真的过该点——三分钟的验算能拦截九成的代数失误。更进一步，考试策略上可以先「猜」后「证」：把直线摆到水平或竖直的特殊位置，交点连线的中点、斜率乘积先算出来，定值是多少、定点在哪里，心里有数之后再走联立流程——目标明确，计算就不迷路。

#section("思维方法", "工程思维：压轴题是一道流水线作业")

#method-box([把浪漫交给定义，把苦活交给程序], [
圆锥曲线压轴的分工：*定义与图形*负责直觉与校验（先猜），*联立与韦达*负责落实与书写（后证）。

考场执行清单：设线带参数 → 联立查判别式 → 韦达写下两个和积 → 代入目标式 → 整理参数结构 → 消参读结论。清单执行质量决定得分下限，定义直觉决定上限。
])

#section("行动清单", "今天就能做")

轨迹问题作为圆锥曲线压轴的第三类，值得补一个方法论标注：*求轨迹 = 求约束的代数翻译*。五大方法各有战场：定义法（动点满足椭圆/抛物线定义）、直译法（几何条件逐句翻译）、参数法（引入中间变量再消去）、交轨法（两条动线的交点）、相关点法（动点随已知曲线上的点联动）。判断用哪个方法的第一问：「动点的约束是直接写得出方程，还是要借助中间量？」——直接则直译，间接则参数。轨迹题看似多变，识别框架后只是五种模板的轮播。

#strong[快问快答：三个高频疑问]
- *定点求出来是字母表达式怎么办？* 答：说明不是定点——回到韦达检查是否漏了某个参数关系，或用特殊值反推。
- *设 $y = k x + b$ 与 $x = m y + b$ 怎么选？* 答：默认后者（防垂直丢解）；仅当题目明确讨论斜率时用前者。
- *弦长公式里的 $sqrt(1 + k^2)$ 哪来的？* 答：把弦长投影到 y 轴：$|"AB"| = sqrt((Delta x)^2 + (Delta y)^2) = |Delta y| sqrt(1 + 1/k^2)$，两种写法等价。

#action-box((
  [① 把联立四步舞默写成流程卡，配上面例题的每一步对照，贴在压轴题错题页首页。],
  [② 用特殊位置法猜三道「定值」题的答案（取直线平行对称轴、过顶点等特殊情形），再反推证明思路。],
  [③ 重做上面例题，限时 25 分钟，重点检查判别式限制与垂直条件的翻译是否写全。],
))
