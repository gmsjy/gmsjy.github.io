#let title = "概率：给随机一个刻度"
#let date = "2026-09-02"
#let tags = ("数学", "概率", "高中")
#let series = "高中数学"
#let series_weight = 18
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

= 随机现象与样本空间

*随机试验*指满足三个条件的试验：可以在相同条件下*重复*进行；所有可能结果是*明确可知*的；每次试验之前*不能确定*会出现哪个结果。抛一枚硬币、掷一颗骰子都是随机试验。

- *样本空间*：随机试验所有可能结果组成的集合，记 $Omega$，其中每个结果叫*样本点*；
- *随机事件*：样本空间的子集，简称*事件*，常用大写字母 $A$、$B$ 表示。事件发生当且仅当试验结果属于该子集；
- *必然事件*：一定会发生的事件，即 $Omega$ 本身；*不可能事件*：一定不发生的事件，即空集 $emptyset$。

#table(
  columns: (auto, auto, auto),
  inset: 6pt,
  [*事件类型*], [*含义*], [*掷骰子的例子*],
  [随机事件], [可能发生也可能不发生], ["点数为偶数"（$\{2, 4, 6\}$）],
  [必然事件], [每次试验一定发生], ["点数不超过 6"（$Omega$）],
  [不可能事件], [每次试验一定不发生], ["点数为 7"（$emptyset$）],
)

> 易错点：随机试验的结果必须是*明确可知*的——"明天会不会下雨"不是随机试验，因为无法在相同条件下重复；但"掷骰子出现几点"是。

= 古典概型

== 两个条件与计算公式

如果随机试验满足：① 样本空间只有*有限个*样本点；② 每个样本点发生的可能性*相等*，就叫*古典概型*。设事件 $A$ 包含的样本点数为 $n(A)$，样本空间 $Omega$ 的总样本点数为 $n(Omega)$，则：

$ P(A) = (n(A))/(n(Omega)) $ <eq:classical>

== 计数样本点的方法

样本点个数不多时，最可靠的办法是*列全*：列举法（一一写出）、树状图（分步试验，如"先抛硬币再掷骰子"）、列表法（两个对象各取一个，如"两枚骰子"的 $6 times 6$ 表）。分步试验用树状图，乘积型试验用列表。

> 易错点：务必检查*等可能性*！"抛两枚硬币"的样本空间是 $\{$正正、正反、反正、反反$\}$ 四个*等可能*结果，不是三个——"一正一反"实际对应两个样本点，概率是 $1/2$ 而非 $1/3$。

= 事件的关系与运算

#table(
  columns: (auto, auto, auto, auto),
  inset: 6pt,
  [*关系*], [*记号*], [*含义*], [*掷骰子例子（$A$ = 点数奇数）*],
  [包含], [$B subset.eq A$], [$B$ 发生则 $A$ 必发生], [$\{1\} subset.eq \{1, 3, 5\}$],
  [并事件（和）], [$A union B$], [$A$、$B$ 至少一个发生], [$\{1,3,5\} union \{2,4,6\} = Omega$],
  [交事件（积）], [$A inter B$], [$A$、$B$ 同时发生], [点数"既是奇数又是质数" $= \{3, 5\}$],
  [互斥], [$A inter B = emptyset$], [不可能同时发生], [点数为奇数 与 点数为 2],
  [对立], [$B = overline(A)$], [$A$ 与 $B$ 互斥且必有一个发生], [点数为奇数 与 点数为偶数],
)

*互斥*与*对立*是最高频考点：互斥只要求"不能同时发生"（可以都不发生），对立还要求"必有一个发生"（补集关系）。*对立必互斥，互斥不一定对立*。

> 集合语言统一记忆：事件就是集合——并 = 至少一个发生，交 = 同时发生，对立 = 取补集。把概率问题翻译成集合问题，文氏图一画，关系一目了然。

= 概率的基本性质与运算

== 基本性质

对任何事件 $A$：$0 <= P(A) <= 1$；必然事件 $P(Omega) = 1$；不可能事件 $P(emptyset) = 0$。

== 互斥事件与对立事件的概率

若 $A$、$B$ *互斥*（$A inter B = emptyset$），则它们不可能同时发生，概率直接相加：

$ P(A union B) = P(A) + P(B) $ <eq:add-mutex>

由 $A union overline(A) = Omega$ 且互斥，得*对立事件公式*（"正难则反"的总开关）：

$ P(overline(A)) = 1 - P(A) $ <eq:complement>

更一般的*加法公式*（$A$、$B$ 不互斥时，交的部分被加了两次要扣回一次）：

$ P(A union B) = P(A) + P(B) - P(A inter B) $ <eq:add-general>

#table(
  columns: (auto, auto),
  inset: 6pt,
  [*题型信号*], [*处理动作*],
  ["至少有一个…"], [求对立事件"一个都没有"的概率，用 $1 - P$],
  ["至多有一个…"], [求对立事件"至少两个"的概率],
  ["恰好一次 / 都发生"], [直接按古典概型数样本点],
  [两事件不互斥求并], [用一般加法公式，减去交的概率],
)

> 口诀：*"至少"对"一个都没有"，"至多"对"反面情形"*——凡正面情形分类繁琐，一律转对立事件，这是概率题性价比最高的第一步。

= 频率与概率

*频率*是试验做出来的比例（$n$ 次试验中事件 $A$ 出现 $n_A$ 次，频率 $= n_A/n$），随试验次数波动；*概率*是事件固有的客观度量，是一个确定的常数。两者关系：大量重复试验时，频率*稳定于*概率——这就是*频率估计概率*的依据（大数定律的直觉形态）。

#fig(cetz.canvas(length: 0.9cm, {
  // 频率随试验次数增多而稳定于概率 p = 0.5
  line((-0.2, 0), (10.6, 0), mark: (end: ">"), stroke: black)
  line((0, -0.2), (0, 3.2), mark: (end: ">"), stroke: black)
  content((10.4, 0.45), $ n $, size: 9pt)
  content((0.5, 3.05), "频率", size: 8pt)
  line((0, 1.5), (10.2, 1.5), stroke: (paint: rgb("#3c8a4d"), dash: "dashed"))
  content((9.6, 1.75), $ P = 0.5 $, size: 8pt)
  let pts = range(0, 21).map(k => {
    let x = 0.5 + k * 0.48
    (x, 1.5 + 0.9 * calc.pow(-0.62, k))
  })
  line(..pts, stroke: (paint: rgb("#d64541"), thickness: 1.3pt))
  for p in pts { circle(p, radius: 0.05, fill: rgb("#d64541"), stroke: none) }
  content((5.2, -0.7), "试验次数越多，频率波动越小，越来越贴近概率", size: 8pt)
}))

#table(
  columns: (auto, auto),
  inset: 6pt,
  [*频率*], [*概率*],
  [由试验得到，随试验次数变化], [理论值，客观存在、不变],
  [是"已经发生的比例"], [是"应该发生的把握"],
  [试验次数越大，越接近概率], [由事件本身决定，与试验无关],
)

> 总结：统计是"从数据反推规律"（用样本估计总体），概率是"由规律预言数据"（用模型计算可能性）——两章合起来构成高中"数据分析"素养的完整闭环：*样本 → 统计 → 推断 → 概率模型 → 预测*。
