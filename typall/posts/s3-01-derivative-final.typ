#let title = "导数压轴：零点、极值与不等式证明"
#let date = "2026-08-27"
#let tags = ("数理衔接", "数学", "高考压轴")
#let series = "压轴与先修"
#let series_weight = 1
#let draft = false

#import "../assets/series-kit.typ": *

#text(size: 8.5pt, fill: gray-line)[高考压轴 · 01 / 18 ｜ 阅读约 15 分钟]

// s3-01-derivative-final.typ — 第三季 01：导数压轴：零点、极值、不等式证明
#import "../assets/series-figures/s3-fig-01-derivative-final.typ": fig as fig-family
#import "../assets/series-figures/s3-fig-01b-zero-existence.typ": fig as fig-zero
#import "../assets/series-figures/s3-fig-01c-tangent-bound.typ": fig as fig-tangent

#section("痛点引子", "只写了 a = 0 的那道 12 分题")

高三一模，倒数第二题：已知实数 $a$，讨论函数 $f(x) = ln x - a x$ 的零点个数。阅卷抽样显示，近三成考生只讨论了 $a = 0$ 一种情况，还有人通篇在「求 $f(x) = 0$ 的解」——一个根本求不出解析解的方程。

换个角度想：这道题如果参数 $a$ 提前给定，比如 $a = 1$，几乎人人能做——求导、列表、下结论，一气呵成。真正卡住所有人的，是「$a$ 可以取任何实数」这个自由度：它意味着答案不再是一个结论，而是一张*分情形的地图*。压轴题考的从来不是「会不会求导」——人人都求得出——而是两样更稀缺的东西：*面对参数的讨论纪律*，和*把问题变成图像的构造眼光*。这一篇用一个例题贯穿「零点讨论」，再用「切线放缩」打开不等式证明的门。

#section("知识地图", "从会算导数到会用导数论证")

#gap-table((
  ([会求初等函数的导数、会列表], [含参函数的分类讨论、零点个数的完整论证], [「分类标准怎么定」没有人系统教过]),
  ([会用 $f' > 0$ 证单调性], [零点存在性定理的规范表述 + 唯一性论证], [「存在」与「唯一」要分开说，表达断层]),
  ([会证 $f(x) > g(x)$ 型简单题], [极值点偏移、切线放缩、构造函数证明], [从「作差」到「构造」的思路跃迁]),
))

#section("核心讲解", "零点讨论的完整流程")

#strong[核心概念一：把「零点个数」变成「图像交点」。]

方程 $ln x - a x = 0$ 永远解不出解析解，出路是*参变分离*：把它改写成 $ln x / x = a$。于是「$f(x)$ 的零点个数」变成「曲线 $g(x) = ln x / x$ 与水平线 $y = a$ 的交点个数」。$g(x)$ 在 $x = e$ 处取得最大值 $1 / e$，一张图定乾坤：

#figure-block("1-1", [函数族 $f(x) = ln x - a x$：水平线 $y = a$ 与 $g(x)$ 的交点决定零点个数], fig-family)

四类情形一目了然：$a <= 0$ 时一个零点；$0 < a < 1 / e$ 时两个；$a = 1 / e$ 时恰好相切、一个；$a > 1 / e$ 时没有。*「相切」在图像上对应极值点恰好落在 $x$ 轴上——这就是分类标准 $1 / e$ 的来历*：所有讨论的「界」，都长在图像的「关节」上。

若不分离参数、直接对 $f(x)$ 讨论，同样可行：$f'(x) = 1 / x - a$，按 $a$ 的符号分情况求最大值 $f(1/a) = ln(1/a) - 1$，比较它与 $0$ 的大小——你会到达同样的四个分支。两条路都要会：分离参数擅长「零点个数」，直接讨论擅长「含 $f$ 不等式」。

#strong[核心概念二：零点存在性定理——「存在」与「唯一」分开说。]

#figure-block("1-2", [零点存在性定理的图像语言：$f(a) f(b) < 0$ 保证存在，单调性保证唯一], fig-zero)

证明「恰有两个零点」的规范句式是三段论：

+ *存在性*：$f$ 在 $[x_1, x_2]$ 上连续，且 $f(x_1) f(x_2) < 0$，由零点存在性定理，$f$ 在 $(x_1, x_2)$ 内存在零点；
+ *唯一性*：$f'$ 在该区间内恒正（或恒负），$f$ 严格单调，故零点至多一个；
+ *不漏*：在其余区间上 $f$ 恒正（或恒负），无零点。

阅卷现场，大量试卷丢分不在思路而在表述：只说「由图像知」、不验证连续与端点异号、存在唯一混在一句话里。压轴题的表述本身就是考点。

顺带把「*极值点偏移*」的套路也拆给你看——它是零点讨论最出名的续集。典型设问：$f$ 有两个零点 $x_1 < x_2$，证明 $x_1 x_2 > e^2$。直接处理乘积无从下手，先取对数翻译成 $ln x_1 + ln x_2 > 2$；再利用极值点 $x = e$ 是两个零点的「对称轴心」这一图像事实，把 $ln x_2$ 折到 $e$ 左侧的对称位置 $2 e - ln x_1$ 作比较；最后构造 $g(t) = ln t - ln(2 e - t)$，说明它在 $(0, e)$ 上递减即可收尾。整套动作的关键词只有三个：*翻译条件、对称扯平、构造单调*。第一遍看不懂很正常，把这句抄在错题本上，第二遍重做时你会突然看懂。

#section("例题拆解", "四类讨论完整版 + 一次放缩")

#problem-box([例 1（零点讨论）], [讨论 $f(x) = ln x - a x$（$a in RR$）的零点个数。])

定义域 $(0, +oo)$。$f'(x) = 1 / x - a$。

- *当 $a <= 0$*：$f'(x) > 0$ 恒成立，$f$ 单调递增；$f(1) = -a >= 0$，且 $x -> 0^+$ 时 $f -> -oo$，故恰有一个零点；
- *当 $a > 0$*：$f$ 在 $(0, 1/a)$ 增、$(1/a, +oo)$ 减，最大值 $f(1/a) = ln(1/a) - 1$。
  - 若 $0 < a < 1 / e$：$f(1/a) > 0$，且两端极限均为 $-oo$，故恰有两个零点；
  - 若 $a = 1 / e$：$f(1/a) = 0$，最大值恰在 $x$ 轴上，恰有一个零点 $x = e$；
  - 若 $a > 1 / e$：$f(1/a) < 0$，恒小于零，无零点。

每个分支三件套齐全：*导数符号、最值符号、端点（极限）行为*。少一件，讨论就不完整。

再备一手高考压轴的常用暗器：*隐零点*。有时令 $f'(x) = 0$ 之后解不出 $x_0$ 的解析解，比如 $f'(x) = ln x + x - 3 = 0$。此时不要恋战：设零点为 $x_0$，把关系式 $ln x_0 = 3 - x_0$ *原封不动地保留在答卷上*，随后一切涉及 $f(x_0)$ 的化简都用这条关系代入。例如 $f(x_0) = x_0 + 1 - 3 x_0 = 1 - 2 x_0$——含 $ln$ 的表达式凭空消失，最值的符号便可以估计出来。阅卷标准明确规定：设而不求、整体代换是满分做法。很多学生卡在「必须解出 $x_0$」的执念上，白白丢掉半道题；请记住，压轴题里*关系比数值值钱*。

#problem-box([例 2（切线放缩）], [证明：当 $x > 1$ 时，$ln x < x - 1$。])

先认识两件「放缩神器」，它们来自两条最简单的切线：

$ e^x >= x + 1 quad (x = 0" 处取等"), quad quad ln x <= x - 1 quad (x = 1" 处取等") $

#figure-block("1-3", [切线放缩：曲线永远在切线上方（左），$ln x$ 永远在切线下方（右）], fig-tangent)

以右边为例，证明只需构造差函数：令 $phi(x) = ln x - x + 1$，则 $phi'(x) = 1 / x - 1$，在 $(0, 1)$ 为正、$(1, +oo)$ 为负，故 $phi$ 在 $x = 1$ 处取最大值 $phi(1) = 0$，从而 $ln x <= x - 1$ 恒成立。而当 $x > 1$ 时 $x - 1 > 0$ 且 $ln x < x - 1$ 中的等号仅在 $x = 1$ 处取到，故严格不等，证毕。

更快的路：令 $t = ln x$，要证 $t < e^t - 1$，正是左边那条放缩 $e^t >= t + 1$ 的严格版——*两条神器互为反函数，一条会证，另一条免费*。

放缩的使用心法是「*该紧则紧，该松则松*」：目标不等式越弱，允许的放缩越粗糙；目标越精细，就要选在取等点附近最贴合的切线。比如要证 $e^x > x^2 + 1$（$x > 0$），用 $e^x >= x + 1$ 就不够贴——$x + 1$ 压不住 $x^2 + 1$——这时要么改用更高阶的放缩 $e^x >= x^2 / 2 + x + 1$，要么老老实实回去构造差函数。判断「贴不贴」的办法很朴素：把两边在取等点附近各算两三个值比一比。

#section("思维方法", "压轴题的三条纪律")

#method-box([纪律先行，技巧随后], [
*纪律一：定义域与分类标准先行。* 动笔先写定义域；分类标准不重不漏，且每个「界」都要能指着图像说出来历（如 $1 / e$ 对应相切）。

*纪律二：数形结合定位讨论点。* 相切 = 最值 = 0，是所有含参讨论的「关节」。先画草图找到关节，再动笔算。

*纪律三：证不了就构造与放缩。* 证 $f > g$，先作差 $h = f - g$ 求最值；求不出最值，用切线放缩把 $f$ 压低、把 $g$ 抬高。压轴题的第一问几乎人人能拿，纪律决定你能不能续上第二问。
])

#section("行动清单", "今天就能做")

#action-box((
  [① 默画四情形：不看本篇，把 $f(x) = ln x - a x$ 在 $a <= 0$、$0 < a < 1/e$、$a = 1/e$、$a > 1/e$ 四种情形的草图各画一遍，标注最大值与零点。],
  [② 手证两件套：用构造差函数的方法完整证明 $e^x >= x + 1$ 与 $ln x <= x - 1$，并用后者独立证明例 2，注意取等条件的表述。],
  [③ 重写一次模考压轴：取出最近一次模考的导数压轴题，按「求导 → 定分类标准 → 逐类三件套 → 合并结论」四个栏目重写在错题本上，缺哪栏补哪栏。],
))
