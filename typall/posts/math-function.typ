#let title = "函数的概念与性质：单调性、奇偶性与图像变换"
#let date = "2026-09-02"
#let tags = ("数学", "函数", "高中")
#let series = "高中数学"
#let series_weight = 3
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

函数是高中数学的*绝对主线*——从集合到导数，几乎每一章都在围绕"函数"打转。这一篇把函数的定义、三要素、单调性与奇偶性一次讲透。

= 函数的概念与三要素

函数是"输入—输出"的对应关系：设 $A, B$ 是两个非空数集，如果按照某种确定的对应关系 $f$，对 $A$ 中的*任意*一个数 $x$，在 $B$ 中都有*唯一确定*的数 $y$ 与它对应，那么就称 $f: A arrow.r B$ 为从集合 $A$ 到集合 $B$ 的一个*函数*。

*三要素*：定义域 $A$、对应关系 $f$、值域 $f(A) = {f(x) | x in A}$。

> 核心：*定义域优先*。讨论函数的一切性质（单调、奇偶、值域）前，先写出定义域。两函数相同 $<=>$ 定义域与对应关系都相同（与用什么字母无关）——$y = x$ 与 $y = x^2/x$ 定义域不同，不是同一函数。

== 求定义域的常见约束

#table(
  columns: (auto, 1fr),
  inset: 6pt,
  [*形式*], [*约束条件*],
  [分式 $1/(f(x))$], [分母 $f(x) != 0$],
  [偶次根式 $root(2n, f(x))$], [被开方数 $f(x) >= 0$],
  [对数 $log_(f(x)) g(x)$], [真数 $g(x) > 0$；底数 $f(x) > 0$ 且 $f(x) != 1$],
  [零次幂 $(f(x))^0$], [底数 $f(x) != 0$],
  [正切 $tan f(x)$], [$f(x) != pi/2 + k pi, k in ZZ$],
)

实际题目往往是多个约束的*交集*：先分别写出每个约束，再取公共部分（画数轴最直观）。

= 函数的表示法与分段函数

函数的三种表示：解析法、图像法、列表法。求解析式常用三种技巧：

- *待定系数法*：已知函数类型（一次、二次…），设出通式代入求系数；
- *换元法*：已知 $f(g(x))$，令 $t = g(x)$ 反解 $x$；
- *方程组法*：已知 $f(x)$ 与 $f(1/x)$（或 $f(-x)$）的关系，联立消元。

*分段函数*在定义域的不同子集上有不同解析式，求值要"对号入座"先判断 $x$ 落在哪一段；分段函数的单调性要逐段讨论且注意*分段点处的衔接*。

> 易错点：$f(x + 1)$ 与 $f(x)$ 的定义域关系——若 $f(x)$ 定义域是 $[0, 1]$，则 $f(x + 1)$ 中 $x + 1 in [0, 1]$，解得 $x in [-1, 0]$。"括号内的东西整体受同一个定义域约束"，这是抽象函数定义域问题的总钥匙。

= 单调性与最大（小）值

== 单调性的定义（用定义证明的范式）

设 $f(x)$ 定义域为 $I$，区间 $D subset.eq I$。对任意 $x_1, x_2 in D$ 且 $x_1 < x_2$：若 $f(x_1) < f(x_2)$，则 $f$ 在 $D$ 上*单调递增*；若 $f(x_1) > f(x_2)$，则 $f$ 在 $D$ 上*单调递减*。

> 证明单调性的五步范式（必须掌握）：① 任取 $x_1 < x_2 in D$；② 作差 $f(x_1) - f(x_2)$；③ 变形（因式分解、通分、有理化——目标是"判号"）；④ 定号（结合 $x_1 - x_2 < 0$ 判断整个式子符号）；⑤ 下结论。

== 判断单调性的快捷方法

- 图像法：从左到右"上坡"递增、"下坡"递减；
- 运算性质：增 $+$ 增 $=$ 增，减 $+$ 减 $=$ 减（*增与减相加不定*）；
- 复合函数"同增异减"：$y = f(g(x))$ 的单调性看内层与外层单调性是否一致，一致则增、相反则减；
- 导数法（后期核心）：$f'(x) > 0$ 则增，$f'(x) < 0$ 则减。

== 最大（小）值

$f(x_0)$ 是最大值 $<=>$ 对一切 $x in I$ 有 $f(x) <= f(x_0)$，且存在 $x_0 in I$ 取到。求最值：优先单调性；二次函数看对称轴与区间位置关系（*轴动区间定 / 轴定区间动*的分类讨论是重点题型）。

= 奇偶性

#fig(cetz.canvas(length: 0.9cm, {
  // 奇函数：图像关于原点对称，(x, f(x)) 与 (-x, -f(x)) 成对出现
  line((-0.2, 1.8), (5.1, 1.8), mark: (end: ">"), stroke: black)
  line((2.4, -0.3), (2.4, 3.7), mark: (end: ">"), stroke: black)
  content((5.0, 2.15), $ x $, size: 9pt)
  content((2.75, 3.6), $ y $, size: 9pt)
  content((2.15, 1.55), $ O $, size: 8pt)
  let pts = range(0, 41, step: 1).map(k => {
    let x = (k / 40.0 - 0.5) * 4.4
    (2.4 + x, 1.8 + 0.125 * x * x * x)
  })
  line(..pts, stroke: (paint: rgb("#d64541"), thickness: 1.4pt))
  circle((4.0, 2.31), radius: 0.07, fill: rgb("#0b6fc0"), stroke: none)
  circle((0.8, 1.29), radius: 0.07, stroke: rgb("#0b6fc0"), fill: none)
  line((4.0, 2.31), (0.8, 1.29), stroke: (paint: rgb("#0b6fc0"), dash: "dashed"))
  content((4.35, 2.65), $ (x, f(x)) $, size: 8pt)
  content((0.55, 0.85), $ (-x, -f(x)) $, size: 8pt)
  content((4.55, 1.3), $ f(-x) = -f(x) $, size: 8pt)
}))

== 定义与判定

设 $f(x)$ 定义域关于原点对称。若对任意 $x$ 都有 $f(-x) = f(x)$，则 $f$ 为*偶函数*；若 $f(-x) = -f(x)$，则 $f$ 为*奇函数*。

判定三步骤：① 定义域关于原点对称（不对称直接非奇非偶）；② 求 $f(-x)$ 并与 $f(x)$ 比较；③ 下结论。既奇又偶的函数只有一个：$f(x) = 0$（定义域关于原点对称时）。

== 图像与运算性质

#table(
  columns: (auto, 1fr),
  inset: 6pt,
  [*性质*], [*要点*],
  [图像对称性], [偶函数图像关于 $y$ 轴对称；奇函数图像关于原点对称],
  [奇函数+原点], [若奇函数在 $x = 0$ 处有定义，则 $f(0) = 0$（高频！）],
  [单调性], [奇函数在对称区间上单调性相同；偶函数相反],
  [四则运算], [奇 $plus.minus$ 奇 $=$ 奇，偶 $plus.minus$ 偶 $=$ 偶，奇 $times$ 偶 $=$ 奇，奇 $times$ 奇 $=$ 偶，偶 $times$ 偶 $=$ 偶],
  [复合], [$y = f(g(x))$：$f$ 偶则整体偶；$f$ 奇且 $g$ 奇则整体奇],
)

> 高频套路：已知 $f(x)$ 是奇函数、$x > 0$ 时 $f(x) = x^2 - 2 x$，求 $x < 0$ 的解析式——令 $x < 0$，则 $-x > 0$，$f(x) = -f(-x) = -(x^2 + 2x) = -x^2 - 2x$。*先补变量到已知区间，再用奇偶性转换*。

= 周期性（进阶储备）

存在非零常数 $T$ 使 $f(x + T) = f(x)$ 对定义域内一切 $x$ 成立，则 $T$ 是周期。三个常用等价条件：

- $f(x + a) = f(x) arrow.r.double T = a$；
- $f(x + a) = -f(x) arrow.r.double T = 2a$（可推广：$f(x+a) = 1/(f(x))$ 也是 $T = 2a$）；
- $f(x + a) = f(x - a) arrow.r.double T = 2a$。

> 对称性两大结论（选择填空秒杀）：① 若 $f(x)$ 关于 $x = a$ 与 $x = b$ 都轴对称（$a != b$），则周期 $T = 2 |a - b|$；② 若关于 $(a, 0)$ 与 $x = b$ 对称（一中心一轴），则 $T = 4 |a - b|$。抽象函数题"给两条对称性求周期"全靠这个。
