#let title = "热学与气体：从分子动理论到理想气体状态方程"
#let date = "2026-09-04"
#let tags = ("物理", "热学", "高中")
#let series = "高中物理"
#let series_weight = 9
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

前面十几课都在跟「看得见摸得着」的宏观运动打交道。这一课钻到物体*内部*：一块铁、一罐气体，里面亿万个分子在做什么？答案出人意料地简单——它们在做*永不停歇的无规则运动*。热学的全部宏观规律，都是这亿万次微观碰撞的统计结果。

= 分子动理论：热现象的微观图像

== 物体由大量分子组成

一滴水里有多少个水分子？$10^21$ 个量级——比全世界人口还多十几个数量级。把摩尔数与分子数连起来的桥是*阿伏伽德罗常数*：

$ N_A = 6.02 times 10^23 "mol"^-1 $ <eq:na>

有了它就能做*分子尺度的估算*（选择题高频考点）：把分子看成一个小球（或一个立方体），直径数量级 $10^-10 m$。

#block(fill: rgb("#f4f1ea"), inset: 12pt, radius: 6pt)[
*例*：水的摩尔体积约 $18 "cm"^3 \/ "mol"$，估算一个水分子的直径。

把每个分子占的体积看成立方体：$V_0 = V_m \/ N_A$，边长即直径——

$ d = root(3, (18 times 10^-6) / (6.02 times 10^23)) approx 3 times 10^-10 m $

注意这是「占位体积」的边长，比真实分子略大；数量级对就行。
]

== 分子永不停歇的无规则运动：布朗运动

1827 年布朗在显微镜下看到悬浮在水中的花粉小颗粒*无规则地游走不停*。颗粒本身没有生命，谁在推它？——周围水分子从四面八方撞它，撞击*不平衡*时颗粒就被踹了一脚。

布朗运动的三个要点：

- *间接证据*：它反映的是*水分子*的无规则运动，不是花粉本身的性质；
- *温度越高越剧烈*：分子的平均动能随温度升高而增大；
- *颗粒越小越明显*：小颗粒在任一瞬间受到的撞击数少，不平衡更容易显现。

> 记忆点：温度是分子*平均动能*的标志。温度相同的氧气和氢气，分子平均动能相同，但氢气分子质量小，平均速率更大。

== 分子间同时存在引力和斥力

分子间既有引力（否则物体一拉就断）又有斥力（否则压不扁）。合力随分子间距 $r$ 变化：

- $r = r_0$（约 $10^-10 m$ 量级）：引力 = 斥力，合力为零，是「平衡位置」；
- $r < r_0$：斥力增长更快，合力表现为*斥力*（表现为「压不动」）；
- $r > r_0$：合力表现为*引力*（表现为「拉不断」）；
- $r > 10 r_0$：作用力趋于零——*气体*正是这种情况，所以气体能无限充满容器。

= 气体实验定律：三个「控制变量」的产物

一定质量的气体，状态由压强 $p$、体积 $V$、温度 $T$ 三个量描述。固定其中一个，研究另外两个，得到三条定律（$T$ 用*热力学温标*，单位 K，$T = t + 273.15$）：

== 玻意耳定律（等温）

$ p_1 V_1 = p_2 V_2 quad (T, m "不变") $ <eq:boyle>

压强与体积成*反比*。微观解释：体积压缩 → 分子更密集 → 单位时间撞单位面积器壁的次数增多 → 压强增大。

== 查理定律（等容）

$ p / T = "恒量" quad (V, m "不变") $ <eq:charles>

微观解释：升温 → 分子平均动能增大 → 每次撞击更「狠」→ 压强增大。

== 盖-吕萨克定律（等压）

$ V / T = "恒量" quad (p, m "不变") $ <eq:gay>

微观解释：升温分子变「猛」，要让压强不变，只能让密度变小（体积变大）来补偿撞击次数的减少。

= 理想气体状态方程：三定律的统一

#fig(cetz.canvas(length: 0.85cm, {
  // p-V 图上的三条等温线（双曲线 pV=const）
  let temps = ((9, rgb("#0b6fc0"), 1), (13, rgb("#3c8a4d"), 2), (17, rgb("#d64541"), 3))
  for t in temps {
    let pts = range(20, 62,step: 4).map(k => (k / 10.0, t.at(0) / (k / 10.0)))
    line(..pts, stroke: t.at(1))
    content((5.4, t.at(0) / 5.0 + 0.1), $ T_#t.at(2) $, anchor: "west")
  }
  // 坐标轴
  line((0, 0), (7.2, 0), mark: (end: ">"), stroke: black)
  line((0, 0), (0, 5.2), mark: (end: ">"), stroke: black)
  content((7.0, 0.42), $ V $)
  content((0.45, 5.0), $ p $)
  // 等温线 T_1 上的一点 A(3,3)：3×3 = 9 恰在该曲线上
  circle((3.0, 3.0), radius: 0.09, fill: black, stroke: none)
  content((3.15, 3.3), $ A $, anchor: "west")
}))

图中每条曲线都是一条*等温线*：$p V = $ 恒量（双曲线），温度越高等温线越「抬高」——同一体积下温度高压强就大（$A$ 点所在的是最低温那条）。三条实验定律其实是同一个方程的特例：

$ (p V) / T = "恒量" quad <==> quad (p_1 V_1) / T_1 = (p_2 V_2) / T_2 $ <eq:state>

配标准形式（$n$ 为摩尔数，$R = 8.31 J \/ "mol" dot K$ 为普适气体恒量）：

$ p V = n R T $ <eq:ideal>

> 什么叫*理想气体*？——分子看成质点、除碰撞外无相互作用的气体模型。实际气体在*常温常压*下非常接近理想气体；低温高压下偏差变大。解题时题目默认理想气体。

#block(fill: rgb("#f4f1ea"), inset: 12pt, radius: 6pt)[
*例*：一体积 $2 L$ 的轮胎在 $27 deg C$（$300 K$）时压强为 $2.5 "atm"$。暴晒后升温到 $57 deg C$（$330 K$），体积近似不变，求新压强。

等容过程用查理定律：$p_2 = p_1 T_2 \/ T_1 = 2.5 times 330 \/ 300 = 2.75 "atm"$。

*易错*：温度必须换成热力学温标再代入——直接用 $27\/57$ 就是经典错误。
]

= 解题方法论

- *认过程*：先判断哪个量不变——等温用 @eq:boyle，等容用 @eq:charles，等压用 @eq:gay；三个都变用 @eq:state。
- *统一单位*：$T$ 一律换 K；$p$ 两边用同一单位即可，不必都换成 Pa。
- *图像题*：$p-V$ 图上*等温线是双曲线*；$p-T$ 图上等容线是*过原点的直线*（延长线；低温段实际气体液化后定律不适用）；$V-T$ 同理。

微观视角一句话总结：*压强来自撞击（密度 × 平均动能），温度就是平均动能的量度*——两条线索交叉，就能定性解释一切气体现象。
