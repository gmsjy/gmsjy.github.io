#let title = "导数综合应用：单调性、极值与含参讨论"
#let date = "2026-09-04"
#let tags = ("数学", "导数", "高中")
#let series = "高中数学"
#let series_weight = 16
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

上一课建立了导数的概念与求导法则。这一课把导数用出来——它是研究函数的*全自动分析仪*：一阶导管单调，二阶导管凹凸，零点管极值。高考压轴题的「含参讨论」不过是把这台机器在参数的每个取值区间上各开一遍。

= 单调性：导数的符号判决

核心逻辑一句话：在区间 $I$ 上

$ f'(x) > 0 ==> f(x) "在" I "上单调递增" quad quad f'(x) < 0 ==> "单调递减" $ <eq:mono>

使用流程标准化：

+ 求*定义域*（定义域优先，忘写必扣分）；
+ 求 $f'(x)$ 并*因式分解*（把符号信息摆到明面上）；
+ 解 $f'(x) > 0 \/ < 0$ 得增减区间，列表呈现。

#fig(cetz.canvas(length: 0.9cm, {
  // 三次函数 f(x)=x^3-3x：单调区间与极值点、切线
  // 坐标轴
  line((-0.6, 0), (6.4, 0), mark: (end: ">"), stroke: black)
  line((0, -2.6), (0, 3), mark: (end: ">"), stroke: black)
  content((6.3, -0.45), $ x $)
  content((0.35, 2.9), $ y $)
  // 曲线 y = (x-1)^3 - 3(x-1)（平移后的 x^3-3x，极值点在 x=2±1 即 (1,±2)）
  let pts = range(-20, 21,step: 1).map(k => {
    let t = k / 10.0
    (t + 3.0, t * t * t - 3 * t)
  })
  line(..pts, stroke: (paint: rgb("#d64541"), thickness: 1.5pt))
  // 极值点
  circle((2.0, 2.0), radius: 0.08, fill: black, stroke: none)
  circle((4.0, -2.0), radius: 0.08, fill: black, stroke: none)
  content((1.7, 2.35), $ (2, 2) $, size: 8pt)
  content((3.6, -2.5), $ (4, -2) $, size: 8pt)
  // 单调区间标注
  content((0.9, 0.4), "增", size: 9pt)
  content((3.0, -0.5), "减", size: 9pt)
  content((5.4, 0.4), "增", size: 9pt)
  // 切线（过极大点，斜率 0 的水平线暗示 f'=0）
  line((0.6, 2.0), (3.4, 2.0), stroke: (paint: rgb("#0b6fc0"), dash: "dashed"))
}))

如图，$f(x) = (x - 3)^3 - 3(x - 3)$ 在 $x = 2$ 处切线*水平*（$f' = 0$），先增后减——*极大值点*；在 $x = 4$ 处先减后增——*极小值点*。

> 记忆点：$f'(x_0) = 0$ 只是*候选*信号，不是充分条件。反例：$f(x) = x^3$ 在 $x = 0$ 处 $f' = 0$ 但不取极值（一直增）。判定极值必须看*导数是否变号*：左正右负 → 极大；左负右正 → 极小。

== 极值与最值：一字之差

- *极值*：局部概念，一个函数可有多个极值，比较的是「邻居」；
- *最值*：全局概念，闭区间上的连续函数必有最值，只需比较*所有极值点 + 两个端点*的函数值。

= 含参数讨论：压轴题的「分类学」

参数一出现，$f'(x)$ 的符号就不再一目了然——分类讨论由此而来。分类的*依据*全部来自方程与不等式，标准清单：

== 讨论点一：二次项系数是否为零

$f(x) = x^3 - a x^2$ 型问题中若求导后出现 $a x$，先问 $a = 0$？此时导数恒非负，函数单调——这一类往往最容易被漏掉。

== 讨论点二：判别式与根的存在

导数为零的方程*有没有根*、根*在不在定义域内*，直接决定函数是「单调」还是「有极值」。

== 讨论点三：根的大小（多根排序）

两个根 $x_1 < x_2$ 都在定义域内时，还要讨论它们与定义域端点的相对位置——*谁大谁小*决定每段区间上的符号。

#fig(cetz.canvas(length: 0.9cm, {
  // 含参讨论：导数零点有无 → 图象形态完全不同
  // (a) 导数有两个零点：增-减-增
  line((0.2, 0), (3.6, 0), mark: (end: ">"), stroke: (paint: rgb("#999"), thickness: 0.8pt))
  let up = range(0, 25,step: 1).map(k => {
    let t = k / 12.0 - 1.0
    (t + 1.9, 2.2 * (t * t * t - t))
  })
  line(..up, stroke: (paint: rgb("#d64541"), thickness: 1.3pt))
  content((1.9, -1.4), "(a) 导数两零点在域内：增-减-增", size: 8pt)
  // (b) 导数无零点：恒增
  line((4.8, 0), (8.2, 0), mark: (end: ">"), stroke: (paint: rgb("#999"), thickness: 0.8pt))
  let low = range(0, 25,step: 1).map(k => {
    let x = 4.8 + k / 8.0
    (x, 0.12 * calc.exp(0.75 * (x - 4.8)) - 0.1)
  })
  line(..low, stroke: (paint: rgb("#3c8a4d"), thickness: 1.3pt))
  content((6.5, -1.4), "(b) 导数无零点：恒增", size: 8pt)
}))

同一个含参函数，参数落到不同区间，图象形态完全不同——*先画草图再写分类*，分类标准自然浮现。

= 恒成立与能成立：转化为最值

恒成立问题是导数的另一大战场，核心转化：

$ AA x in I, f(x) >= lambda quad <==> quad f(x)_("min") >= lambda quad quad EE x in I, f(x) >= lambda quad <==> quad f(x)_("max") >= lambda $ <eq:hold>

分离参数是首选技巧：把 $lambda$ 挪到一边，问题变成「$g(x) >= lambda$ 恒成立」→ 求 $g(x)$ 的最小值。

#block(fill: rgb("#f4f1ea"), inset: 12pt, radius: 6pt)[
*例*：已知 $AA x > 0$，$x e^x >= a x$ 恒成立，求 $a$ 的取值范围。

$x > 0$，两边*同除以正数* $x$（不翻转不等号）：$e^x >= a$ 恒成立。

问题化为「$a$ 不超过 $e^x$ 在 $(0, +oo)$ 上的下界」：$e^x$ 递增且 $e^x > e^0 = 1$，故 $a <= 1$。

*验端点*：$a = 1$ 时需要 $x e^x >= x$，即 $e^x >= 1$——对一切 $x > 0$ 成立，故等号可取：$a in (-oo, 1]$。

*注*：$e^x$ 在开区间上取不到最小值 1，但「$a$ 不超过它的下界」依然合法——判断等号能否取，只需要*回代验证*，不需要最值真的被取到。
]

= 方程的根与函数零点：数形结合的终点站

研究 $f(x) = g(x)$ 的解的个数，标准动作：*构造* $F(x) = f(x) - g(x)$，讨论 $F$ 的单调极值 → 画草图 → 数图象交点。导数给出图象的「骨架」（升降与峰谷），零点个数由骨架与 $x$ 轴的相对位置读出。

= 解题方法论

- *定义域写在最前*：求导、解不等式、定极值，每一步都在定义域内进行；
- *导数先因式分解*：$f'(x) = 2x(x-1)e^x$ 的符号一眼可读，展开式则什么都看不出来；
- *分类讨论不重不漏*：按「系数为零 → 有无根 → 根比大小」三级清单走，讨论完在草稿上画一条数轴核对参数区间；
- *极值必验变号*：$f' = 0$ 的点逐个检查左右符号；
- *恒成立先分离*：能分离参数就分离，剩下的是纯最值计算；分不掉时考虑构造函数 + 端点效应。

一句话收束：导数研究的全部秘密在于*用代数不等式读出图象形状*——单调是骨架，极值是关节，含参讨论只是把每种可能的骨架都摆上桌面。
