#let title = "概率统计压轴：随机过程、决策与建模"
#let date = "2026-08-30"
#let tags = ("数理衔接", "数学", "高考压轴")
#let series = "压轴与先修"
#let series_weight = 4
#let draft = false

#import "../assets/series-kit.typ": *

#text(size: 8.5pt, fill: gray-line)[高考压轴 · 04 / 18 ｜ 阅读约 14 分钟]

// s3-04-probability-final.typ — 第三季 04：概率统计压轴：随机过程、决策与建模
#import "../assets/series-figures/s3-fig-04-prob-final.typ": fig as fig-markov
#import "../assets/series-figures/s3-fig-04b-decision.typ": fig as fig-decision
#import "../assets/series-figures/s3-fig-04c-expectation.typ": fig as fig-expect

#section("痛点引子", "「考了两年的概率题，怎么突然看不懂了」")

老一套的概率题：摸球、抽样、二项分布，套路清晰。新高考的概率压轴却换了一副面孔：*甲乙轮流投篮直到有人先进球*、*每次考试状态受上次影响*、*给出规则求第 n 轮某人获胜概率*。条件一大段，事件一层套一层，很多考生读完题连第一问都列不出。

这类题的共同名字叫*随机过程*——概率不再是「一次性事件」，而是「随时间演化的状态」。它们背后的通用钥匙只有一把：马尔可夫链的递推思想。这一篇把它拆给你看。

#section("知识地图", "从「算一次概率」到「追踪状态演化」")

已学基础：分布列、期望方差、条件概率、全概率公式（第二季第 7 篇）。压轴新增：*状态转移*的递推表示、马尔可夫链的稳态、期望决策。断层在哪？单步概率人人会算，压轴考的是「第 n 步的概率 $P_n$」——它需要*用第 $n - 1$ 步表示第 $n$ 步*，本质是概率版的数列递推（第三季第 3 篇的方法论在这里整体迁移）。

#figure-block("4-1", [状态转移图：下一状态只依赖当前状态], fig-markov)

状态转移图是随机过程的「受力分析图」：节点是状态（答对/答错、赢/输、下雨/晴天），箭头是转移概率。画图的纪律：每个状态的所有出箭头概率之和必须为 1。这张图的价值在于把一段绕口的文字题翻译成清晰的代数关系——压轴题的第一问几乎总是「写出 $P_(n+1)$ 与 $P_n$ 的关系」，答案就藏在图里。

#section("核心讲解", "两个核心：递推方程与稳态求解")

#strong[核心概念一：用全概率公式建立递推。]

设 $P_n$ 为「第 $n$ 轮答对」的概率。按上图：第 $n + 1$ 轮答对，要么本轮从 A 出发以 $2/3$ 留在 A，要么从 B 出发以 $1/2$ 转入 A。由全概率公式：

$ P_(n+1) = 2/3 dot P_n + 1/2 (1 - P_n) = 1/2 dot P_n + 1/2 $

看到这个形状，数列训练立刻接管：*一阶线性递推*，用待定系数构造法（第二季第 3 篇）解出 $P_n$，或直接进入稳态分析。概率压轴的第一问到此解决——它考的与其说是概率，不如说是把概率翻译成递推的能力。

#strong[核心概念二：稳态 = 递推的「不动点」。]

#figure-block("4-2", [期望决策树：用期望比较两套策略], fig-decision)

稳态概率 $P^*$ 满足 $P^* = 1/2 P^* + 1/2$，解得 $P^* = 1$？不对——回代检查：$P^* = (1/2 P^* + 1/2)$，即 $P^* / 2 = 1/2$，$P^* = 1$。再核对转移图：留在 A 的概率 2/3、从 B 转入 1/2，稳态应介于两者之间——方程写错了：正确的是 $P_(n+1) = 2/3 P_n + 1/2 (1 - P_n) = 1/6 + 1/2 P_n$，稳态 $P^* = 1/6 + 1/2 P^*$，$P^* = 1/3$。*稳态求解只要两行，但前提是转移方程抄对*——压轴题丢分常在粗枝大叶处。

#figure-block("4-3", [期望随回合振荡收敛到稳态], fig-expect)

稳态的几何意义是「振荡收敛」的落点（见图）。而期望决策（图 4-2）则是另一类压轴场景：给多套策略算期望再比较。它的纪律是*穷举所有分支、每个分支写概率与收益、逐层加权求和*——决策树就是「分布列 + 期望」的组合应用，没有新知识，只有新耐心。

#section("例题拆解", "一道传球游戏的完整解法")

#problem-box([例题], [三人传球，每球随机传给另外两人之一。球从 A 开始，求第 $n$ 次传球后球回到 A 手中的概率 $P_n$。])

*建模*：球在谁手里只有两种「状态」——在 A 手中、不在 A 手中。转移关系：在 A → 必然传给别人（回 A 概率 0）；不在 A → 传回 A 概率 $1/2$。

*递推*：$P_(n+1) = 0 dot P_n + 1/2 (1 - P_n) = 1/2 - 1/2 P_n$。

*求解*：稳态 $P^* = 1/3$（对称性验证：球最终等概率落在三人手中 ✓）。通项 $P_n = 1/3 + (2/3)(-1/2)^(n-1)$——用待定系数法把递推化成等比（构造 $P_n - 1/3$）。

*复盘*：这道近年真题的全部考点——状态抽象、全概率递推、构造法解通项、对称性验证——都是旧知识的重新编排。概率压轴的「新」只是新皮肤，骨架仍然是递推与构造。

概率压轴还有一个「验证纪律」值得单独立规矩：*递推结果必须三重验账*。其一，初始值代入通项验算 $P_1$；其二，极限与对称性对照（传球问题稳态 $1/3$，三人对称 ✓）；其三，量级合理性（概率必须落在 0 与 1 之间，且稳态不能超过条件概率的最大值）。三验的成本两分钟，拦截的是整个大题的崩盘——概率题的答案错误几乎无法从卷面自查，验账是唯一的保险丝。

#section("思维方法", "建模思维：把过程写成状态机")

#method-box([随机过程三问], [
拿到过程类概率题，先问三句：*有哪些状态？*（要互斥且穷尽）*转移概率是多少？*（每个状态出箭头概率和为 1）*初始状态是什么？*

三问答完，递推方程自动浮现。这套「状态机思维」不仅是高考概率压轴的钥匙，也是计算机科学、金融建模、流行病学的通用语言——它是本系列最值得带进大学的一件行李。
])

#section("行动清单", "今天就能做")

全概率与贝叶斯是随机过程的一对「前进与倒车」：全概率公式顺推「原因 → 结果」（由本轮状态算下轮），贝叶斯公式逆推「结果 → 原因」（已知答错了，反推上一轮在哪个状态）。新高考已明确把贝叶斯列入考查视野，它的操作要点是分母写全（所有原因的总概率）。贝叶斯思维的日常化身是医学检测的解读：检测准确率 99% 不等于「阳性者 99% 有病」——当疾病本身罕见时，阳性人群中真患者的比例可能远低于直觉。*条件概率的反直觉，是概率思维成熟的标志。*

#strong[快问快答：三个高频疑问]
- *状态要设几个才够？* 答：若「下一状态的分布」只依赖当前分类，最少就够；若不够，细化状态（加记忆维度）。
- *稳态一定存在吗？* 答：高考范围内的有限状态、可互通链都存在且唯一——课纲不考周期链的例外。
- *期望大就选期望大的方案吗？* 答：风险中性时是；若「输不起」（一次性决策），还要看方差——期望与风险要一起看。

#action-box((
  [① 把传球例题独立重做一遍，重点练「画状态转移图 → 写全概率递推」这两步的书写规范。],
  [② 求上面例子 $n = 4$ 时的 $P_4$（直接递推），再用通项公式验算，体会两条路线的交叉验证。],
  [③ 找一道期望决策的真题（如「是否换门」问题），画出决策树并计算两策略的期望，比较直觉与计算。],
))
