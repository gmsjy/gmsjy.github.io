#let title = "直线与圆的方程：用代数描绘画布"
#let date = "2026-09-04"
#let tags = ("数学", "解析几何", "直线与圆", "高中")
#let series = "高中数学"
#let series_weight = 13
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

上一站圆锥曲线的主角是椭圆、双曲线、抛物线，可它们的"骨架"其实是更基础的直线与圆。解析几何的核心思想只有一句：*用方程刻画图形，用代数运算代替几何直观*。这篇把"点—直线—圆"三件套讲透，它们是所有解析几何题的入场券。

= 直线的倾斜角与斜率

一条直线向上的方向与 $x$ 轴正方向的夹角 $alpha$（$0 <= alpha < pi$）叫*倾斜角*。斜率 $k$ 是倾斜角的正切：

$ k = tan alpha = (y_2 - y_1)/(x_2 - x_1) $ <eq:slope>

- $alpha = 0$：水平线，$k = 0$；
- $0 < alpha < pi/2$：$k > 0$，随 $alpha$ 增大而增大；
- $pi/2 < alpha < pi$：$k < 0$；
- $alpha = pi/2$：*竖直直线斜率不存在*（$x_2 = x_1$），这是最容易丢分的地方。

> 易错点：倾斜角是"直线向上的方向与 $x$ 轴正方向"的夹角，范围 $[0, pi)$；斜率不是"角度越大越大"，$tan alpha$ 在 $pi/2$ 处断——$k$ 从正无穷跳到负无穷。

= 直线方程的四种形式

#table(
  columns: (auto, auto, 1fr),
  inset: 6pt,
  [*形式*], [*方程*], [*适用 / 已知条件*],
  [点斜式], [$y - y_0 = k (x - x_0)$], [一点 + 斜率（最常用）],
  [斜截式], [$y = k x + b$], [斜率 + 纵截距],
  [两点式], [$(y - y_1)/(y_2 - y_1) = (x - x_1)/(x_2 - x_1)$], [两点（$x_1 != x_2$）],
  [一般式], [$A x + B y + C = 0$], [所有直线统一形式，$A$、$B$ 不同时为 $0$],
)

> 口诀：*"点斜斜截两点式，最后统统化一般"*。解题时先选合适形式，最后结果写成一般式最保险（避免漏掉斜率不存在的竖直直线——点斜式、斜截式天然排除了它）。

由一般式 $A x + B y + C = 0$ 可读出：斜率 $k = -A/B$，纵截距 $b = -C/B$。

= 两直线的位置关系与距离

== 平行与垂直（用系数判断）

$l_1: A_1 x + B_1 y + C_1 = 0$，$l_2: A_2 x + B_2 y + C_2 = 0$：

- *平行*：$A_1 B_2 = A_2 B_1$ 且 $B_1 C_2 != B_2 C_1$（系数成比例但常数项不成比例）；
- *垂直*：$A_1 A_2 + B_1 B_2 = 0$；
- 用斜率表示更直白：$k_1 = k_2$ 平行，$k_1 k_2 = -1$ 垂直（*斜率存在*时）。

== 距离公式

- *两点间距离*：$abs(P_1 P_2) = sqrt((x_2 - x_1)^2 + (y_2 - y_1)^2)$；
- *点到直线距离*：点 $P(x_0, y_0)$ 到直线 $A x + B y + C = 0$：

$ d = (abs(A x_0 + B y_0 + C))/(sqrt(A^2 + B^2)) $ <eq:dist>

- *两平行线距离*：$C_1$、$C_2$ 对应两平行线，$d = (abs(C_1 - C_2))/(sqrt(A^2 + B^2))$。

> 记忆：分子是把点代进直线方程取绝对值，分母是 $A$、$B$ 平方和开根——"*点代进去取绝对，系数平方开根号*"。

= 圆的方程

== 标准方程与一般方程

圆心 $C(a, b)$、半径 $r$ 的*标准方程*：

$ (x - a)^2 + (y - b)^2 = r^2 $ <eq:circle>

展开得*一般方程* $x^2 + y^2 + D x + E y + F = 0$（需 $D^2 + E^2 - 4 F > 0$），此时圆心 $(-D/2, -E/2)$、半径 $r = 1/2 sqrt(D^2 + E^2 - 4 F)$。

> 易错点：① 一般方程必须"配方"回去才看得出圆心半径，公式记牢：圆心是 $(-D/2, -E/2)$（*系数一半取负*）；② 圆方程是二元二次且 $x^2$、$y^2$ 系数相同、无 $x y$ 项——这是"是不是圆"的快速判据。

直线与圆的三种位置关系，只看圆心到直线的距离 $d$ 与半径 $r$ 的大小：

#fig(cetz.canvas(length: 0.9cm, {
  // 相交 / 相切 / 相离
  circle((1.3, 1.5), radius: 0.95, stroke: rgb("#0b6fc0"))
  line((0.1, 1.3), (2.5, 1.3), stroke: (paint: rgb("#d64541"), thickness: 1.2pt))
  content((1.3, -0.3), "相交", size: 8pt)
  circle((3.9, 1.5), radius: 0.95, stroke: rgb("#0b6fc0"))
  line((2.7, 0.55), (5.1, 0.55), stroke: (paint: rgb("#d64541"), thickness: 1.2pt))
  line((3.9, 1.5), (3.9, 0.55), stroke: (paint: rgb("#9aa2ad"), dash: "dashed"))
  content((3.9, -0.3), "相切", size: 8pt)
  circle((6.5, 1.5), radius: 0.95, stroke: rgb("#0b6fc0"))
  line((5.3, 2.95), (7.7, 2.95), stroke: (paint: rgb("#d64541"), thickness: 1.2pt))
  line((6.5, 1.5), (6.5, 2.95), stroke: (paint: rgb("#9aa2ad"), dash: "dashed"))
  content((6.5, -0.3), "相离", size: 8pt)
  content((2.75, 2.75), $ d < r $, size: 8pt)
  content((5.35, 2.75), $ d = r $, size: 8pt)
  content((7.5, 2.2), $ d > r $, size: 8pt)
}))

== 直线与圆的位置关系（几何法最优）

圆心到直线的距离 $d$ 与半径 $r$ 比较：

- $d < r$：*相交*（弦长 $= 2 sqrt(r^2 - d^2)$）；
- $d = r$：*相切*（切点即垂足，切线唯一）；
- $d > r$：*相离*。

> 判断直线与圆的位置关系，*永远先用几何法（比 $d$ 与 $r$）*，不要硬联立解判别式——几何法快且直观。只有求具体交点坐标时才联立方程。

== 圆的切线

过圆上一点 $P(x_0, y_0)$ 的切线：圆心与 $P$ 连线垂直于切线，切线垂直半径。过圆外一点作圆有*两条*切线（求切点用"切点在圆上 + 半径垂直切线"两个条件）。

= 圆与圆的位置关系

两圆圆心距 $d$，半径 $R$、$r$（$R >= r$）：

#table(
  columns: (auto, auto, 1fr),
  inset: 6pt,
  [*位置*], [*条件*], [*公共点*],
  [外离], [$d > R + r$], [$0$],
  [外切], [$d = R + r$], [$1$],
  [相交], [$abs(R - r) < d < R + r$], [$2$],
  [内切], [$d = abs(R - r)$], [$1$],
  [内含], [$d < abs(R - r)$], [$0$],
)

两圆相交时，两方程相减得到*公共弦所在直线方程*（一次方程），用它配合点到直线距离求公共弦长，比求交点快得多。

= 小结

- 斜率 $k = tan alpha = (y_2 - y_1)/(x_2 - x_1)$，竖直直线斜率不存在；
- 直线方程点斜式最常用，最终化一般式防漏；平行看系数成比例，垂直看 $A_1 A_2 + B_1 B_2 = 0$；
- 点到直线距离公式 $d = (abs(A x_0 + B y_0 + C))/(sqrt(A^2 + B^2))$ 是解析几何的"万能尺"；
- 圆：标准方程找圆心半径，一般方程配方回去；直线与圆、圆与圆位置关系一律用"圆心距 vs 半径"的几何法判断；
- 求切线、弦长时记住"半径垂直切线、垂径定理"两个几何武器。
