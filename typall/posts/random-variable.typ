#let title = "随机变量及其分布：把随机性变成可计算的数"
#let date = "2026-09-04"
#let tags = ("数学", "概率", "随机变量", "高中")
#let series = "高中数学"
#let series_weight = 21
#let draft = false

#import "@preview/cetz:0.5.2"
#import cetz.draw: *
#import "../assets/preview.typ": fig, eq-numbering
#show math.equation.where(block: true): set math.equation(numbering: eq-numbering)
#show math.equation.where(block: false): set math.equation(numbering: none)

抛一枚硬币，结果是"正面/反面"；抽一件产品，可能是"合格/不合格"。这些结果的共同点是不能预先确定——但它们的*规律*是可以算的。随机变量就是把"随机结果"映射成"数"，让我们能用期望、方差去量化随机性。这篇把分布列、三大常见分布、期望与方差一次讲清。

= 随机变量：给随机结果"编号"

把随机试验的每一个结果都用一个实数表示，这个变量叫*随机变量*，记作 $X$（如 $X = 1$ 表示"正面"、$X = 0$ 表示"反面"）。

- *离散型随机变量*：取值可以一一列举（如掷骰子的点数 $1,2,...,6$）；
- *连续型随机变量*：取值充满某个区间、无法一一列举（如某地降雨量、电子元件的寿命），用密度函数描述。

> 概念辨析：随机变量的"值"不是"概率"，而是"结果对应的数"。设 $X$ 表示"掷两颗骰子点数之和"，$X = 7$ 是事件，$P(X = 7)$ 才是概率——别把二者混为一谈。

= 离散型随机变量的分布列

== 定义与性质

$X$ 的所有可能取值为 $x_1, x_2, ..., x_n$，对应概率 $p_1, p_2, ..., p_n$，列表：

#fig(cetz.canvas(length: 0.9cm, {
  // 分布列的图形形态：竖线高度的「质量」集中在期望 E(X) 附近
  line((-0.2, 0), (6.2, 0), mark: (end: ">"), stroke: black)
  line((0, -0.2), (0, 2.9), mark: (end: ">"), stroke: black)
  content((6.05, 0.4), $ x_i $, size: 9pt)
  content((0.5, 2.75), $ p_i $, size: 9pt)
  let stems = ((1, 0.6), (2, 1.2), (3, 2.2), (4, 1.2), (5, 0.6))
  for s in stems {
    line((s.at(0), 0), (s.at(0), s.at(1)), stroke: (paint: rgb("#0b6fc0"), thickness: 1.4pt))
    circle((s.at(0), s.at(1)), radius: 0.07, fill: rgb("#0b6fc0"), stroke: none)
  }
  line((3, 0), (3, 2.55), stroke: (paint: rgb("#d64541"), dash: "dashed"))
  content((3.35, 2.6), $ E(X) $, size: 9pt)
  content((3.2, -0.6), "概率像质量压在各点上，支点恰在期望处", size: 8pt)
}))

#table(
  columns: (auto, auto, auto, auto),
  inset: 6pt,
  [$X$], [$x_1$], [$x_2$], [$...$, $x_n$],
  [$P$], [$p_1$], [$p_2$], [$...$, $p_n$],
)

分布列必须满足两条性质：

- $p_i >= 0$（概率非负）；
- $sum p_i = 1$（所有可能结果概率和为 1，*检验分布列对不对先看这条*）。

== 常见离散分布

- *两点分布*（$X tilde B(1, p)$）：$X = 1$ 概率 $p$，$X = 0$ 概率 $1-p$；
- *二项分布*（$X tilde B(n, p)$）：$n$ 次独立重复试验（成功概率 $p$），恰好成功 $k$ 次的概率 $P(X = k) = C_n^k p^k (1-p)^(n-k)$；
- *超几何分布*：$N$ 件产品中有 $M$ 件次品，不放回抽 $n$ 件，恰有 $k$ 件次品，$P(X = k) = (C_M^k C_(N-M)^(n-k))/(C_N^n)$。

#table(
  columns: (auto, auto, auto, auto),
  inset: 6pt,
  [*分布*], [*模型*], [*概率*], [*期望*],
  [两点], [一次试验], [$P(X = 1) = p$], [$p$],
  [二项], [*有放回* $n$ 次独立重复], [$C_n^k p^k (1-p)^(n-k)$], [$n p$],
  [超几何], [*不放回* 抽 $n$ 件], [$(C_M^k C_(N-M)^(n-k))/(C_N^n)$], [$n M/N$],
)

> 一句话区分：*"有放回（独立重复）→ 二项分布；不放回（一次抽足）→ 超几何分布"*。当总体很大、抽取很少时，超几何分布近似二项分布。

= 期望（均值）：随机变量的"平均取值"

离散型随机变量 $X$ 的*数学期望*（均值）是各取值与其概率的加权平均：

$ E(X) = x_1 p_1 + x_2 p_2 + ... + x_n p_n = sum x_i p_i $ <eq:expect>

期望的两条运算性质（常用来化简）：

- $E(a X + b) = a E(X) + b$（线性变换，$a$、$b$ 为常数）；
- $E(X + Y) = E(X) + E(Y)$（和的期望 = 期望的和，与是否独立无关）。

> 例题感：甲乙两人各掷一枚骰子，甲得点数为 $X$（$E(X) = 3.5$），若规则改为"甲的点数乘以 2 再减 1"，则 $E(2X - 1) = 2 times 3.5 - 1 = 6$。*期望是"线性"的，直接套公式即可，不用重算分布列*。

= 方差：随机变量的"波动程度"

*方差*描述取值偏离期望的程度：

$ D(X) = sum (x_i - E(X))^2 p_i = E(X^2) - (E(X))^2 $ <eq:var>

标准差 $sigma(X) = sqrt(D(X))$。方差越小，取值越集中、越稳定。

方差的两条运算性质：

- $D(a X + b) = a^2 D(X)$（*常数 $b$ 平移不改变波动，系数 $a$ 要平方*）；
- $D(X)$ 越大 → 随机性越强、稳定性越差。

#table(
  columns: (auto, auto),
  inset: 6pt,
  [*分布*], [*方差*],
  [两点分布 $B(1,p)$], [$p(1-p)$],
  [二项分布 $B(n,p)$], [$n p (1-p)$],
  [超几何分布], [与期望搭配，可用公式（略）],
)

> 易错点：① 方差算的是"离差的平方"的期望，不是"期望的平方"；② $D(a X + b) = a^2 D(X)$ 里 *$b$ 完全不参与*（平移不改变波动）；③ 用 $E(X^2) - (E(X))^2$ 算方差时，$E(X^2)$ 是"取值平方"再乘概率求和，别写成 $E(X)$ 的平方。

= 正态分布（连续型，结论速记）

若随机变量 $X$ 服从正态分布 $N(mu, sigma^2)$，其密度曲线呈*钟形*，关于 $x = mu$ 对称，$sigma$ 决定"胖瘦"（$sigma$ 越大曲线越矮胖、数据越分散）。*$3 sigma$ 原则*：

- $P(mu - sigma < X < mu + sigma) approx 0.683$；
- $P(mu - 2 sigma < X < mu + 2 sigma) approx 0.954$；
- $P(mu - 3 sigma < X < mu + 3 sigma) approx 0.997$。

> 记忆：*"68-95-99.7"*——一个标准差内约 68%、两个约 95%、三个约 99.7%。求正态分布落在某区间的概率，先把区间"对齐"到 $mu plus.minus k sigma$ 再利用对称性与这组数。

= 小结

- 随机变量把随机结果"数字化"，离散型用分布列描述（$p_i >= 0$、$sum p_i = 1$）；
- 三大分布：两点、二项（有放回独立重复）、超几何（不放回）；
- 期望 $E(X) = sum x_i p_i$ 是平均取值，性质 $E(a X+b) = a E(X)+b$；
- 方差 $D(X) = E(X^2) - (E(X))^2$ 是波动程度，性质 $D(a X+b) = a^2 D(X)$；
- 二项分布 $E = n p$、$D = n p(1-p)$，是出题最密的结论；
- 正态分布记"68-95-99.7"和钟形对称。
