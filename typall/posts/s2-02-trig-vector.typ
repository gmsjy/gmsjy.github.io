#let title = "三角与向量：周期、解三角形与矢量运算"
#let date = "2026-08-10"
#let tags = ("数理衔接", "数学")
#let series = "高中深化"
#let series_weight = 2
#let draft = false

#import "../assets/series-kit.typ": *

#text(size: 8.5pt, fill: gray-line)[数学深化 · 02 / 18 ｜ 阅读约 13 分钟]

// s2-02-trig-vector.typ — 第二季 02：三角与向量：周期、解三角形与矢量运算
#import "../assets/series-figures/s2-fig-02-trig-vector.typ": fig as fig-circle
#import "../assets/series-figures/s2-fig-02b-sine.typ": fig as fig-sine
#import "../assets/series-figures/s2-fig-02c-dot.typ": fig as fig-dot

#section("痛点引子", "「sin 不是 0.5 吗，怎么变成一条曲线」")

初中的三角函数住在直角三角形里：对边比斜边，永远是 0 到 1 之间的数。高一末它突然变成了一条波浪线，自变量成了任意实数——角度竟然可以「转很多圈」？更令人困惑的是旁边还站着一位新同学「向量」：带箭头的数，能加能乘，规矩全都陌生。

这一篇把这对「旋转与方向」的组合讲通：单位圆如何把三角函数从三角形里解放出来，向量又如何成为描述方向的标准语言——它们俩合起来，就是高中物理的力与振动、高中数学的解三角形与周期函数。

#section("知识地图", "从边比到圆上点，从数到箭头")

初中已学：直角三角形的 sin、cos、tan（锐角）。高中要求：任意角三角函数（单位圆定义）、图像与周期性、诱导公式、正余弦定理解三角形；向量则从零起步——线性运算、数量积、坐标表示。断层在哪？初中三角是「静态的边比」，高中三角是「动态的圆周运动」；向量则是全新的对象，初中学过的力、位移等概念在这里第一次获得严格的数学身份。

#figure-block("2-1", [单位圆定义：角的三角函数 = 圆上点的坐标], fig-circle)

单位圆是三角函数的「重生之地」：半径为 1，角 $theta$ 的终边与圆交于点 $(cos theta, sin theta)$——三角函数从边比变成坐标，角度不再受锐角限制，转几圈都行。左图中的橙色虚线就是「正弦线」：它随 $theta$ 增大而上下起伏，把它按角度展开，就得到正弦曲线。

#figure-block("2-2", [正弦曲线：关键点、周期与值域], fig-sine)

#section("核心讲解", "两个核心：周期性与数量积")

#strong[核心概念一：周期性——每转一圈，历史重演。]

正弦曲线最深刻的性质是 $sin(x + 2 pi) = sin x$：函数值以 $2 pi$ 为周期无限重复。这一条决定了它的所有考法：给 $sin(x + pi / 3)$ 的图像，本质上只是把正弦曲线*向左平移*；求 $y = A sin(omega x + phi)$ 的周期，答案是 $2 pi / omega$。物理上的匀速圆周运动、弹簧振子、交流电，全是这条曲线的化身——「周期」是描述一切往复运动的第一词汇。

#strong[核心概念二：数量积——把两个方向的关系变成一个数。]

#figure-block("2-3", [数量积：向量 b 在向量 a 方向上的投影], fig-dot)

向量加法遵循三角形法则（第 9 篇的力合成已经见过），而数量积 $bold(a) dot bold(b) = |bold(a)||bold(b)| cos theta$ 是新工具：它把两个向量的「夹角关系」压缩成一个数。几何上它是投影的乘积，物理上它就是功的公式 $W = bold(F) dot bold(s)$。两个高频考点直接由它导出：*垂直* $bold(a) dot bold(b) = 0$（垂直）；夹角 $cos theta = (bold(a) dot bold(b)) / (|bold(a)||bold(b)|)$。坐标化后全部变成乘法与加法——向量把「方向问题」翻译成了「计算问题」。

#section("例题拆解", "正弦定理的一个完整应用")

#problem-box([例题], [△ABC 中，$a = 4$，$B = 45 degree$，$C = 60 degree$。求 $c$ 与外接圆半径 $R$。])

角和定理先给 $A = 75 degree$。由正弦定理 $a / sin A = c / sin C$：

$ c = a dot (sin C) / (sin A) = 4 dot (sqrt(3) / 2) / (sin 75 degree) $

其中 $sin 75 degree = sin(45 degree + 30 degree) = sqrt(6) / 4 + sqrt(2) / 4$（两角和公式）。代入化简得 $c = (2 sqrt(3)) / (sin 75 degree) = 4 sqrt(2) (sqrt(3) - 1)$。最后 $2R = a / sin A$ 给出外接圆直径。

复盘用到的工具链：角和定理（三角形内角）→ 正弦定理（边角互化）→ 两角和公式（拆角）。*解三角形的全部套路就是「边角互译」*：看到边就想想换成角，看到角就想想换成边，而翻译词典正是正弦定理与余弦定理。

诱导公式是三角函数的「语法」，而死记十六个公式的代价太高。用单位圆重推一遍，你会发现所有诱导公式都来自三种对称：*关于原点对称*给出 $pi + alpha$ 组（正负号全变），*关于 x 轴对称*给出 $-alpha$ 组（函数名不变、符号看象限），*关于 y 轴对称*给出 $pi - alpha$ 组。画一个圆、点一个点、照一次镜子——公式当场重生。强基与高考都偏爱「推导能力优于记忆量」的考查方式，这张对称图就是你的推导引擎。

#section("思维方法", "旋转思维与投影思维")

#method-box([两个新直觉], [
*旋转直觉*：遇到三角函数，先想单位圆上的点在转。诱导公式不用背——「奇变偶不变，符号看象限」的本质是圆的对称性，画个圆全推出来。

*投影直觉*：遇到两个向量，先想「一个在另一个上的影子有多长」。垂直 = 投影为零，功 = 力的投影，数量积的正负只看影子朝向。
])

#section("行动清单", "今天就能做")

解三角形还有一个「定形条件」的概念值得提前掌握：给出什么条件能唯一确定三角形？三边（SSS）、两边夹角（SAS）、两角一边（ASA/AAS）唯一确定；但「两边对角」（SSA）可能无解、一解或两解——这就是著名的*多解陷阱*。判别的几何方法是画圆弧看交点个数，代数方法是比较对边与高的关系。高考选择题几乎年年在这个点上设卡，提前标记这个「雷区」，考场上就能优雅绕行。

#strong[快问快答：三个高频疑问]
- *三角函数值会超过 1 吗？* 答：正弦余弦不会（单位圆坐标的界）；但正切可以取任意实数——它不是「边的比」的界。
- *向量能用数轴表示吗？* 答：数轴只能装同向或反向的向量；任意方向的向量要坐标系（分量）来养。
- *数量积是负的说明什么？* 答：夹角是钝角——投影朝反方向。零则垂直。

#strong[延伸阅读]
- [S1-09 · 矢量启蒙]：力与位移的矢量直觉先修。
- [S2-10 · 万有引力]：圆周运动的物理主场。
- [S3-08 · 振动与波]：周期函数的物理化身。

#action-box((
  [① 手绘单位圆，标出 $0, pi/6, pi/4, pi/3, pi/2$ 五个角的正弦线与余弦线，并据此写出特殊角的全部三角函数值。],
  [② 默画正弦曲线并标注五个关键点，再画出 $y = sin(x + pi/2)$，验证它就是余弦曲线。],
  [③ 用数量积证明勾股定理的向量版：$bold(a) perp bold(b) ==> |bold(a) + bold(b)|^2 = |bold(a)|^2 + |bold(b)|^2$。],
))
